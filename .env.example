# 本地开发环境标记文件模板
#
# 复制本文件为仓库根目录的 .env 即可把当前源码目录标记为开发环境：
#     Windows:  copy .env.example .env
#     bash:     cp .env.example .env
#
# 开发环境下后端不会向 Sentry 上报错误与性能数据，也不会自行申请 UAC 提权，
# 并且监听 36164 而非正式版的 36163，因此可以与已安装的正式版同时运行。
# .env 不纳入版本库；更新器也不会把它复制到用户安装目录，
# 因此用户侧直接启动后端始终按生产环境处理。
#
# 想临时按生产环境验证上报链路时，删除或重命名 .env 即可。
#
# 由 AUTO-MAS-Runtime 监督启动时（进程环境变量 AUTO_MAS_SUPERVISED=1）上述
# 开发环境行为不生效：端口只认 Runtime 注入的 AUTO_MAS_SUPERVISED_PORT
# （managed 缺省 36163、development 缺省 36164；缺失或非法时回退 36163），
# 关闭请求（/api/core/close）真实生效，优先级高于本文件、AUTO_MAS_ENV 与
# AUTO_MAS_HTTP_PORT。
AUTO_MAS_ENV=development
